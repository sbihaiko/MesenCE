# ADR-0097: Bloco C item 9 replaces SetReplacementMute(bool) with a per-channel mask driven by ChannelRoleClassifier — no ADR records this.

- Status: proposed
- Date: 2026-08-26

## Context
Raised during decompose

## Decision
Record an ADR (id above 0091) for the mute-mask contract: who owns the mask, behavior when classification is unavailable/mid-hysteresis, and the compatibility story for existing callers.

## Consequences


## Alternatives

