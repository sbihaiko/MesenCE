## L-5c0cf7449e07-000 · docs · 2026-08-27
**Pedido:** H2 — rewrite the unit-tests.yml contract in .github/AGENTS.md (ADR-0131)
**Diagnóstico (scout/spec):** Task title named `unit-tests.yml` as the *subject* of the contract while the task's `files` list held only `.github/AGENTS.md`. The critic read the file name in the title as a deliverable: cycle 3 flagged touching `unit-tests.yml` as a scope violation, cycle 4 flagged *not* touching it as a request-fidelity failure — a contradiction the actor could not satisfy, so T1 stagnated. Same pattern as L-b909cad872b0-000 (anchor named in the task diverges from the resolved files).
**Por que falhamos / como evitar:** A file name in a task title is treated as a deliverable by the fidelity check. When a file is only the topic, keep it out of the title (or say "edit only X"): e.g. "Rewrite the CI contract section of .github/AGENTS.md as invariants per ADR-0131". The branch's `.github/AGENTS.md` was correct and was cherry-picked to main by hand (c970dcf3).
**Escopo:** .github/AGENTS.md · **Status:** advisory · **Reflexão-de:** post-mortem
