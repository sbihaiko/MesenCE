# Keeps the SHA-pinned third-party actions in .github/workflows and
# .github/actions current; each bump arrives as a reviewable PR with the
# new commit SHA and its version comment.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
